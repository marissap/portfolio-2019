import React from "react"

const Bio = () => {
  return (
    <div
      style={{
        fontSize: `20px`,
      }}
    >
      <p style={{ paddingBottom: `40px`, }}>
        I think in <span className="bio-emphasis">systems</span> but build for people by <span className="bio-emphasis">transforming</span> abstract concepts into <span className="bio-emphasis">human-centred</span> applications.
      </p>
    </div>
  )
}

export default Bio
